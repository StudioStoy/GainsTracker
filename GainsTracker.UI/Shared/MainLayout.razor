@using GainsTracker.UI.Components
@inherits LayoutComponentBase
@inject NavigationManager NavManager
<ElementManipulator IsDarkTheme="IsDark"></ElementManipulator>
<link href='https://fonts.googleapis.com/css?family=Inter' rel='stylesheet'>

<div class="page">
    @if (NavManager.Uri.Split("/").Last() != "login" && NavManager.Uri.Split("/").Last() != "register" && NavManager.Uri.Split("/").Last() != "forgot")
    {
        <header class="navigation">
            <NavMenu/>
        </header>

    }

    <main>
        @* <button id="toggleThemeButton" @onclick="ToggleDarkMode">Toggle theme</button> *@

        <article class="content">
            @Body
        </article>
    </main>
</div>

@code {
    [Parameter]
    public bool IsDark { get; set; }

    private void ToggleDarkMode()
    {
        IsDark = !IsDark;
    }

}